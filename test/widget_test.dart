import 'package:flutter_test/flutter_test.dart';
import 'package:planner/main.dart';

void main() {
  test('PlannerApp can be constructed', () {
    expect(const PlannerApp(), isA<PlannerApp>());
  });
}
